<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Data Integrity Group | Private AI Systems</title>
    <style>
        :root { --bg: #0f172a; --card: #1e293b; --accent: #38bdf8; --text: #f8fafc; --dim: #94a3b8; }
        body { margin: 0; font-family: 'Inter', sans-serif; background: var(--bg); color: var(--text); scroll-behavior: smooth; }
        nav { background: #1e293b; padding: 1rem; text-align: center; position: sticky; top: 0; border-bottom: 1px solid #334155; z-index: 10; }
        nav a { color: var(--text); margin: 0 15px; text-decoration: none; font-weight: bold; font-size: 0.9rem; }
        .hero { text-align: center; padding: 100px 20px; }
        .hero h1 { font-size: 3rem; color: var(--accent); margin-bottom: 1rem; }
        .hero p { font-size: 1.25rem; color: var(--dim); max-width: 700px; margin: 0 auto 2rem; }
        .btn { display: inline-block; padding: 1rem 2rem; background: var(--accent); color: #0f172a; text-decoration: none; font-weight: bold; border-radius: 8px; transition: 0.2s; }
        .btn:hover { background: #0ea5e9; transform: translateY(-2px); }
        .container { max-width: 1100px; margin: auto; padding: 40px 20px; }
        .grid { display: grid; gap: 30px; grid-template-columns: repeat(auto-fit, minmax(320px, 1fr)); }
        .card { background: var(--card); padding: 2rem; border-radius: 16px; border: 1px solid #334155; text-align: left; }
        .card h3 { color: var(--accent); margin-top: 0; }
        .price { margin-top: 1.5rem; font-weight: bold; color: var(--accent); font-size: 0.9rem; border-top: 1px solid #334155; padding-top: 1rem; }
        footer { text-align: center; padding: 60px 20px; border-top: 1px solid #334155; color: var(--dim); font-size: 0.9rem; }
    </style>
</head>
<body>

<nav>
    <a href="#hero">Home</a>
    <a href="#solutions">Solutions</a>
    <a href="#contact">Contact</a>
</nav>

<section id="hero" class="hero">
    <h1>Data Integrity Group</h1>
    <p>On-premise, air-gapped AI infrastructure. Your sensitive documents stay 100% offline and under your control.</p>
    <a href="#contact" class="btn">Request Technical Audit</a>
</section>

<div id="solutions" class="container">
    <div class="grid">
        <div class="card">
            <h3>Legal Discovery AI</h3>
            <p>Search thousands of pages of discovery and contracts in seconds. Local deployment ensures client-attorney privilege is never compromised.</p>
            <div class="price">Pilots: $1,500 - $3,000 | Enterprise: $5,000+</div>
        </div>
        <div class="card">
            <h3>Real Estate Intelligence</h3>
            <p>Instant retrieval for deeds, disclosures, and easements. Eliminate manual search time and close deals faster with local data processing.</p>
            <div class="price">Pilots: $1,500 - $3,000 | Enterprise: $5,000+</div>
        </div>
    </div>
</div>

<footer id="contact">
    <p><strong>Data Integrity Group</strong><br>Weston, West Virginia</p>
    <a href="mailto:jshultziii@icloud.com" class="btn" style="background: transparent; border: 1px solid var(--accent); color: var(--accent);">Email Consultant</a>
</footer>

</body>
</html>